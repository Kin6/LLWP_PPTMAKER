export const MODEL_CSS_CONTRACT = Object.freeze({
  canvas: Object.freeze({
    width: "1920px",
    height: "1080px",
    serverRootPadding: "0px",
    safeInset: "72px",
    insetRule: "Apply the 72px safe inset exactly once in the generated composition; never stack a second full-canvas inset wrapper.",
  }),
  scope: "Per-slide CSS only. Theme CSS is server-owned and already locked.",
  selectorRoot: ":slide",
  selectorRules: Object.freeze([
    "Every comma-separated selector branch must start exactly with :slide.",
    "The :slide root must be its own first compound; when content follows it, use only a descendant or child combinator.",
    "Never emit :root, bare selectors, IDs, attribute selectors, pseudo-elements, or additional pseudo-classes.",
    "For differentiated elements, add explicit classes in HTML and select those classes beneath :slide.",
  ]),
  allowedCombinators: Object.freeze(["descendant", ">"]),
  allowedProperties: Object.freeze([
    "display", "position", "inset", "top", "right", "bottom", "left",
    "width", "height", "min-width", "max-width", "min-height", "max-height",
    "grid-template-columns", "grid-template-rows", "grid-column", "grid-row",
    "gap", "row-gap", "column-gap", "align-items", "align-content",
    "justify-items", "justify-content", "place-items", "flex", "flex-direction",
    "flex-wrap", "order", "overflow", "box-sizing", "padding", "padding-top",
    "padding-right", "padding-bottom", "padding-left", "margin", "margin-top",
    "margin-right", "margin-bottom", "margin-left", "border", "border-top",
    "border-right", "border-bottom", "border-left", "border-color",
    "border-top-color", "border-right-color", "border-bottom-color", "border-left-color",
    "border-width", "border-top-width", "border-right-width", "border-bottom-width",
    "border-left-width", "border-style", "border-top-style", "border-right-style",
    "border-bottom-style", "border-left-style", "border-radius", "background",
    "background-color", "color", "font-family", "font-size", "font-weight",
    "line-height", "text-align", "text-transform", "letter-spacing", "opacity",
    "object-fit", "object-position", "aspect-ratio", "white-space", "word-break",
    "z-index", "transform",
  ]),
  allowedValueFunctions: Object.freeze([
    "rgb", "rgba", "hsl", "hsla", "calc", "min", "max", "clamp", "minmax",
    "repeat", "fit-content", "matrix", "matrix3d", "translate", "translatex",
    "translatey", "translatez", "translate3d", "scale", "scalex", "scaley",
    "scalez", "scale3d", "rotate", "rotatex", "rotatey", "rotatez", "rotate3d",
    "skew", "skewx", "skewy", "perspective", "var",
  ]),
  themeTokens: Object.freeze([
    "--deck-bg", "--deck-surface", "--deck-text", "--deck-muted", "--deck-primary",
    "--deck-secondary", "--deck-accent", "--deck-positive", "--deck-negative",
    "--deck-font-sans", "--deck-font-serif", "--deck-title-size",
    "--deck-heading-size", "--deck-body-size", "--deck-caption-size",
    "--deck-radius", "--deck-space", "--deck-grid-gap",
  ]),
  forbiddenFeatures: Object.freeze([
    "at-rules", "url()", "custom properties", "!important", "fixed or sticky positioning",
    "viewport or container-viewport units", "nonzero letter-spacing",
    "var() fallbacks or references outside the exact themeTokens list",
  ]),
  validSelectorExamples: Object.freeze([
    ":slide{display:block}",
    ":slide .title{font-size:var(--deck-title-size)}",
    ":slide header, :slide footer{display:flex}",
  ]),
});
